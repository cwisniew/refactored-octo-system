{
  "server": {
    "started": "Server started on port {{port}}",
    "exiting": "Server exiting...",
    "debug": {
      "route": {
        "add": "New route added for {{- path}}"
      }
    },
    "request": {
      "unknownId": "Request for unknown {{what}}, id = {{id}}"
    }
  },
  "campaign": {
    "starter": {
      "name": "New Campaign"
    },
    "debug": {
      "created": "New Campaign object created with id = {{id}}"
    }
  },
  "gamemap": {
    "starter": {
      "name": "New Map"
    },
    "debug": {
      "created": "game map created with id = {{id}}"
    }
  }
}