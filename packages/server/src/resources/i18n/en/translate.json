{
  "server": {
    "started": "Server started on port {{port}}",
    "wsStarted":  "Started Web Socket listener on port {{port}}",
    "exiting": "Server exiting...",
    "debug": {
      "route": {
        "add": "New route added for {{- path}}"
      }
    },
    "request": {
      "unknownId": "Request for unknown {{what}}, id = {{id}}"
    }
  },
  "campaign": {
    "starter": {
      "name": "New Campaign"
    },
    "debug": {
      "created": "New Campaign object created with id = {{id}}"
    }
  },
  "scene": {
    "starter": {
      "name": "New Scene"
    },
    "debug": {
      "created": "Scene created with id = {{id}}"
    }
  }
}